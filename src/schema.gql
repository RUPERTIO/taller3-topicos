# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

"""Una tarea en el sistema de gestión de proyectos."""
type Task {
  id: ID!
  title: String!
  description: String!
  status: TaskStatus!
  tags: [String!]!
  createdAt: DateTime!
  assignee: String!
  project: String!
}

"""Los diferentes estados por los que puede pasar una tarea."""
enum TaskStatus {
  BACKLOG
  TODO
  IN_PROGRESS
  DONE
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type Query {
  hello: String!
  tasks: [Task!]!
  task(id: ID!): Task!
}

type Mutation {
  createTask(input: CreateTaskInput!): Task!
  updateTask(input: UpdateTaskInput!): Task!
  deleteTask(id: ID!): Boolean!
}

input CreateTaskInput {
  title: String!
  description: String!
  tags: [String!]!
  assignee: String!
  project: String!
}

input UpdateTaskInput {
  id: ID!
  title: String
  description: String
  status: TaskStatus
  tags: [String!]
  assignee: String
  project: String
}